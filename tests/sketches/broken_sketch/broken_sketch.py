# Broken Sketch for Testing
# This sketch intentionally has syntax errors

import drawBot as drawbot

# Set canvas size
drawbot.size(400, 400)

# Missing closing parenthesis - syntax error
drawbot.fill(1, 0, 0
drawbot.rect(50, 50, 100, 100)

# Save result
drawbot.saveImage("broken_output.png")
